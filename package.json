{
  "name": "arya-wtf",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "check": "astro check",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.0.0",
    "@fontsource-variable/archivo": "^5.0.0",
    "@fontsource-variable/unbounded": "^5.0.0",
    "@fontsource/space-mono": "^5.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
